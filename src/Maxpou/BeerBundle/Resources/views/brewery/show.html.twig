{% extends 'base.html.twig' %}

{% block body %}
    <h1>Brewery</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brewery.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brewery.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ brewery.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('brewery_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('brewery_edit', { 'id': brewery.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
